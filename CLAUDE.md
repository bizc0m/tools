# Claude Instructions

Follow the repository rules in `AGENTS.md`.

Important trigger:
- `#km` = transform the provided content into a structured Markdown knowledge entry in `/km/`, then update `/km/index.md`.

